# T1Bridge v1 release gate

Status: **incomplete; private pre-release evidence only**, 2026-09-04. A checked
item has complete evidence in the current tree. An unchecked item remains a
release and publication blocker. This record does not authorize publication.

- [ ] All deferred owner reviews and required hardware acceptance checks closed
  — Pending. Owner review and live hardware acceptance records are not closed.
- [x] Required upstream notices and license texts present
  — Package metadata declares every upstream license. Core and DKMS payloads
  carry project and upstream notices. DKMS revision 10 also includes the full
  GPLv2 text; its staged license files compare byte-for-byte with the sources.
- [x] cargo-deny green on the locked graph (licenses, sources, advisories)
  — `cargo deny --all-features check` passed against [`Cargo.lock`](../../Cargo.lock)
  with the policy in [`deny.toml`](../../deny.toml): advisories, bans, licenses,
  and sources all reported `ok`.
- [x] One-time check from the private workspace: grep the manifest's
      literal identifiers against the tree and git history — zero hits
  — After the private history squash, all nine recorded machine literals have
  zero tree and history hits. Decision 13 retains the maintainer identity.
- [ ] No Apple code, blobs, captures, decompilation output, biometric or
      keybag state anywhere in history (fresh history makes this auditable)
  — Filename, file-type, and size scans passed. These do not establish the
  provenance of embedded source data; content and provenance review remains.
- [x] Required Cupertino, Myna UI, and Inter notices retained
  — All three are in [`THIRD_PARTY_NOTICES.md`](../../THIRD_PARTY_NOTICES.md),
  which is present in the core package.
- [ ] Reproducible build verified; release tags and packages signed
  — Pending. All four [`PKGBUILDs`](../../packaging/arch) and the pinned private
  [`release workflow`](../../.github/workflows/release.yml) exist. The workflow
  requires an annotated tag and pinned source hash, builds twice, compares the
  unsigned packages byte-for-byte, verifies install/reinstall/uninstall in an
  isolated Arch root, and gates signing behind owner approval. Offline package
  lifecycle fixtures have passed, including retention of protected machine
  data and user renderer selection. No exact tagged build, signing key,
  signature, hosted repository, or clean hosted install has passed.
- [ ] Standard libfprint/fprintd compatibility passes on exact release packages;
      core has no standard-stack dependency or shipped direct PAM module
  — Patched libfprint passes 132/132 tests and fprintd passes 560/560 using
  virtual readers, mock D-Bus, and wrapped PAM. Both native duplicate rejection
  and software duplicate cleanup are covered. The final matched
  release-package hardware runbook remains open.
- [ ] **One threat-model and security-review record, backed by negative tests**
      (Decision 10), covering: (a) PAM fail-open behavior and lockout paths;
      (b) auth broker + keybag relay peer authentication, races, and injection;
      (c) `t1-xart-storage` network binding, foreign-peer rejection, and
      interface renaming; (d) the `t1-touchbar-hw` privilege boundary; (e)
      kernel memory safety and teardown; (f) desktop-provider parsing, direct
      execution, deadlines, and environment boundary; (g) camera descriptor
      parsing and buffer bounds; (h) iBridge autosuspend, reset, recovery, and
      concurrent interface lifecycle; (i) UID-0 target-user assertion and
      unprivileged cross-user rejection; (j) libfprint parsing and fprintd's
      Polkit boundary; and (k) every privileged component and open socket.
      Findings and fixes committed. AI may assist but review pass counts are
      not evidence.
  — Pending. The single [`security review`](../security-review/v1.md) exists and
  maps every required boundary and current negative test. Exact-package
  single-machine evidence covers cold boot, fingerprint, PAM, Touch Bar,
  camera, and composite-device recovery. Remaining gaps are stated there.
- [ ] Full hardware runbook pass on **both** machines on the exact release
      artifacts
  — Pending. The active machine passed the revision-58/7/5/1 package stack;
  final release artifacts and the second machine remain open.
- [x] Runtime dependency + supported-version documentation complete
  — The current-tree [`dependency inventory`](../dependencies.md) records the
  exact v1 distribution and architecture scope, hardware- and build-tested
  kernels, minimum and CI Rust toolchains, systemd floor, C toolchain,
  build/test-only `liblzma` boundary, audited runtime linkage, and matched
  fingerprint compatibility versions. Final reproducibility and exact-release
  hardware evidence remain separate gates.
- [x] Core package has no Omarchy, `wpctl`, MPRIS/`busctl`, notification
      utility, provider daemon, or provider socket dependency
  — Source, manifests, package recipe, and the current dependency inventory
  keep desktop implementation downstream. The only provider executable in the
  tree is a Rust process fixture behind an all-targets test feature and is not
  built by the package recipe.
